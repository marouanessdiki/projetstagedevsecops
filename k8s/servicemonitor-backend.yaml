apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: projetstage-backend-monitor
  labels: { release: prometheus }
spec:
  selector:
    matchLabels: { app: projetstage, tier: backend }
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
